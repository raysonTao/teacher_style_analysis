# 架构图快速使用指南

## 📊 生成的三个图及其对应论文位置

### 1. SHAPE网络架构图 (`shape_architecture.tex`)
**插入位置**: 图3.2（Section 3.3.2 SHAPE网络设计）

**展示内容**:
- 输入层：视觉(20维) + 音频(15维) + 文本(35维)
- 模块1：特征投影 → 512×3维
- 模块2：**跨模态注意力**（核心创新）- 展示α_{i→j}权重连接
- 模块3：BiLSTM时序建模 → 1024维
- 模块4：注意力池化
- 模块5：7类风格分类器

**关键标注**:
- 三种模态颜色编码（红/绿/蓝）
- 跨模态注意力箭头显示模态交互
- 残差连接（虚线）
- 图例说明：相比简单拼接准确率提升8.3%

---

### 2. 系统四层架构图 (`system_architecture.tex`)
**插入位置**: 图3.1（Section 3.1.2 系统架构设计）

**展示内容**:
- **Layer 1 数据层**: MySQL + Redis + MinIO
- **Layer 2 特征层**: 三模态Pipeline并行（耗时0.82s/10s片段）
- **Layer 3 推理层**: SHAPE模型(342K参数) + SHAP解释器
- **Layer 4 应用层**: 画像生成 + 可视化 + 分析服务

**关键标注**:
- 异步任务队列（Celery + RabbitMQ）
- 三级缓存策略（L1=24h, L2=7d, L3=永久）
- 水平扩展能力（50并发→200并发）
- 用户接口（Vue.js + ECharts）

---

### 3. 多模态特征提取流程图 (`multimodal_pipeline.tex`)
**插入位置**: 图5-1（Section 5.2.1，可选）

**展示内容**:
- **视频流水线**（0.82s）: ExtractFrames(0.05s) → YOLOv8(0.18s) → DeepSORT(0.12s) → MediaPipe(0.25s) → ST-GCN(0.18s) → F_v∈R^20
- **音频流水线**（0.37s）: LoadAudio(0.05s) → Whisper(0.15s) → Wav2Vec2(0.08s) → Emotion(0.07s) → F_a∈R^15
- **文本流水线**（0.15s）: BERT(0.06s) → H-DAR(0.04s) → ComputeNLP(0.05s) → F_t∈R^35

**关键标注**:
- Pipeline并行处理标记
- 各步骤耗时标注
- 依赖关系（Whisper → 文本流水线）
- 特征合并：F = {F_v, F_a, F_t} ∈ R^70

---

## 🚀 三步编译（推荐Overleaf）

### 方法1: Overleaf在线编译（最快，3分钟）

1. **打开Overleaf**: https://www.overleaf.com/
2. **创建项目**: New Project → Blank Project
3. **逐个编译**:
   - 删除默认内容
   - 复制 `shape_architecture.tex` 内容并粘贴
   - 等待自动编译（约10秒）
   - 下载PDF: Download PDF
   - 重复以上步骤编译另外两个文件

### 方法2: 本地编译（需安装LaTeX）

```bash
# 安装LaTeX（Ubuntu/Debian）
sudo apt-get install texlive-latex-extra texlive-pictures

# 编译��有图
cd /home/rayson/code/teacher_style_analysis/doc/08版本/架构图生成
./compile_all.sh

# 或单独编译
pdflatex shape_architecture.tex
pdflatex system_architecture.tex
pdflatex multimodal_pipeline.tex
```

### 方法3: 转换为高分辨率PNG（用于Word）

编译成PDF后：

```bash
# 安装ImageMagick
sudo apt-get install imagemagick

# 转换为300 DPI PNG
convert -density 300 shape_architecture.pdf -quality 90 shape_architecture.png
convert -density 300 system_architecture.pdf -quality 90 system_architecture.png
convert -density 300 multimodal_pipeline.pdf -quality 90 multimodal_pipeline.png
```

---

## 📝 插入论文的LaTeX代码

### 插入SHAPE架构图（Section 3.3.2）

```latex
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{doc/08版本/架构图生成/shape_architecture.pdf}
    \caption{SHAPE网络架构图 (Semantic Hierarchical Attention Profiling Engine)}
    \label{fig:shape_architecture}
\end{figure}
```

### 插入系统架构图（Section 3.1.2）

```latex
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{doc/08版本/架构图生成/system_architecture.pdf}
    \caption{教师风格画像分析系统四层架构设计}
    \label{fig:system_architecture}
\end{figure}
```

### 插入多模态流程图（Section 5.2.1）

```latex
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{doc/08版本/架构图生成/multimodal_pipeline.pdf}
    \caption{多模态特征提取流程图（对应Algorithm 1）}
    \label{fig:multimodal_pipeline}
\end{figure}
```

---

## ✅ 检查清单

- [ ] 已编译 `shape_architecture.pdf`
- [ ] 已编译 `system_architecture.pdf`
- [ ] 已编译 `multimodal_pipeline.pdf`
- [ ] 已转换PNG（如果使用Word）
- [ ] 已插入图3.1（系统架构）
- [ ] 已插入图3.2（SHAPE网络）
- [ ] 已插入图5-1（多模态流程，可选）
- [ ] 图注与章节号对应正确

---

## 🎯 推荐工作流程

1. **使用Overleaf快速编���**（无需本地安装）
2. **下载PDF** → 直接插入LaTeX论文
3. **或转换PNG** → 插入Word论文（300 DPI高清）
4. **检查论文中的图注引用**（如"见图3.2"）

**预计耗时**: 10分钟完成所有三个图的编译和插入

---

**生成时间**: 2026-02-07
**对应论文**: doc/08版本/论文_08稿.md
**架构图版本**: v1.0
