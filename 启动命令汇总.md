# 教师风格分析系统 - 启动命令汇总

本文档整理了完整的系统启动命令，包括特征提取、VLM标注、模型训练等所有流程。

---

## 📋 目录

1. [单视频测试流程](#单视频测试流程)
2. [批量处理流程](#批量处理流程)
3. [完整训练流程](#完整训练流程)
4. [模型推理](#模型推理)
5. [验证测试](#验证测试)
6. [工具脚本](#工具脚本)

---

## 🎯 单视频测试流程

### 1. 测试特征提取（仅教师数据）

```bash
# 测试新的教师识别功能
python test_teacher_detection.py

# 预期输出：
# - 记录数: ~2,143 条（从17,960减少）
# - 文件大小: ~5-7 MB（从30MB减少）
# - Y坐标范围: 150-300（教师区域）
```

### 2. 提取单个视频特征

```bash
# 方法1: 使用FeatureExtractor（推荐）
python -c "
from src.features.feature_extractor import FeatureExtractor
import json

extractor = FeatureExtractor()
features = extractor.process_video(
    'data/custom/videos/高中语文优质课《声声慢》（含课件教案）.mp4',
    return_raw=True
)

with open('output_features.json', 'w', encoding='utf-8') as f:
    json.dump(features, f, ensure_ascii=False, indent=2)

print('特征提取完成！')
"

# 方法2: 使用命令行工具
python data/custom/tools/extract_features.py \
    --videos_dir data/custom/videos \
    --output data/custom/extracted_features/features_single.json \
    --max_videos 1
```

### 3. VLM标注单个视频

```bash
# 对单个视频进行VLM标注
python data/custom/tools/annotate_videos.py \
    --features_path data/custom/extracted_features/features_single.json \
    --output data/custom/vlm_annotations/annotation_single.json \
    --model opus \
    --max_samples 1

# 查看标注结果
cat data/custom/vlm_annotations/annotation_single.json | jq .
```

---

## 🔄 批量处理流程

### 1. 批量提取特征（多个视频）

```bash
# 提取目录下所有视频的特征
python data/custom/tools/extract_features.py \
    --videos_dir data/custom/videos \
    --output data/custom/extracted_features/features_batch.json \
    --sample_interval 30 \
    --max_videos 0  # 0表示处理所有视频

# 只处理前10个视频
python data/custom/tools/extract_features.py \
    --videos_dir data/custom/videos \
    --output data/custom/extracted_features/features_10.json \
    --max_videos 10
```

**参数说明**：
- `--videos_dir`: 视频目录
- `--output`: 输出JSON文件路径
- `--sample_interval`: 采样间隔（默认30帧）
- `--max_videos`: 最大处理数量（0=全部）

### 2. 批量VLM标注

```bash
# 标注所有样本（使用Opus模型，高质量）
python data/custom/tools/annotate_videos.py \
    --features_path data/custom/extracted_features/features_batch.json \
    --output data/custom/vlm_annotations/annotations_batch.json \
    --model opus \
    --max_samples 0 \
    --num_keyframes 10

# 使用Sonnet模型（更快，稍低成本）
python data/custom/tools/annotate_videos.py \
    --features_path data/custom/extracted_features/features_batch.json \
    --output data/custom/vlm_annotations/annotations_batch.json \
    --model sonnet \
    --max_samples 0
```

**参数说明**：
- `--features_path`: 特征文件路径
- `--output`: 标注输出路径
- `--model`: VLM模型（opus/sonnet）
- `--max_samples`: 最大标注数量（0=全部）
- `--num_keyframes`: 每个样本使用的关键帧数量（默认10）

### 3. 一键批量处理脚本

```bash
# 完整批量处理：特征提取 + VLM标注
bash data/custom/tools/process_custom_videos.sh

# 该脚本会自动：
# 1. 提取所有视频特征
# 2. 使用VLM进行标注
# 3. 生成训练数据格式
```

---

## 🎓 完整训练流程

### 步骤1: 准备训练数据

```bash
# 将VLM标注结果转换为训练格式
python data/custom/tools/convert_to_training.py \
    --annotations data/custom/vlm_annotations/annotations_batch.json \
    --features data/custom/extracted_features/features_batch.json \
    --output data/custom/training_data/training.json \
    --train_ratio 0.7 \
    --val_ratio 0.15 \
    --test_ratio 0.15 \
    --min_confidence 0.5

# 查看训练数据统计
python -c "
import json
with open('data/custom/training_data/training.json') as f:
    data = json.load(f)
    train = [x for x in data if x['split'] == 'train']
    val = [x for x in data if x['split'] == 'val']
    test = [x for x in data if x['split'] == 'test']
    print(f'训练集: {len(train)}')
    print(f'验证集: {len(val)}')
    print(f'测试集: {len(test)}')
    print(f'总计: {len(data)}')
"
```

**参数说明**：
- `--train_ratio`: 训练集比例（默认0.7）
- `--val_ratio`: 验证集比例（默认0.15）
- `--test_ratio`: 测试集比例（默认0.15）
- `--min_confidence`: 最低VLM置信度阈值（默认0.5）

### 步骤2: 训练MMAN模型

```bash
# 使用默认配置训练
python -m src.models.deep_learning.train \
    --data_path data/custom/training_data/training.json \
    --model_config default \
    --batch_size 32 \
    --num_epochs 150 \
    --lr 5e-5 \
    --device cuda \
    --checkpoint_dir ./checkpoints/custom \
    --log_dir ./logs/custom \
    --early_stopping 15

# 使用高精度配置训练
python -m src.models.deep_learning.train \
    --data_path data/custom/training_data/training.json \
    --model_config high_accuracy \
    --batch_size 16 \
    --num_epochs 200 \
    --lr 3e-5 \
    --device cuda \
    --checkpoint_dir ./checkpoints/custom_high \
    --log_dir ./logs/custom_high \
    --early_stopping 20

# CPU训练（较慢）
python -m src.models.deep_learning.train \
    --data_path data/custom/training_data/training.json \
    --model_config default \
    --batch_size 8 \
    --num_epochs 150 \
    --device cpu \
    --checkpoint_dir ./checkpoints/custom_cpu \
    --log_dir ./logs/custom_cpu
```

**参数说明**：
- `--model_config`: 模型配置（default/high_accuracy/lightweight）
- `--batch_size`: 批次大小（GPU推荐32，CPU推荐8）
- `--num_epochs`: 训练轮数
- `--lr`: 学习率
- `--device`: 设备（cuda/cpu）
- `--early_stopping`: 早停轮数

### 步骤3: 监控训练进度

```bash
# 查看训练日志
tail -f logs/custom/training.log

# 查看最新的训练指标
tail -20 logs/custom/training.log | grep "Epoch"

# 使用TensorBoard（如果安装了）
tensorboard --logdir logs/custom
```

---

## 🚀 模型推理

### 1. 使用训练好的模型推理

```bash
# 对新视频进行推理
python -m src.main analyze \
    --video data/custom/videos/new_video.mp4 \
    --mode deep_learning \
    --model_path checkpoints/custom/best_model.pth \
    --device cuda \
    --output results/new_video_result.json

# 批量推理
python -m src.main analyze \
    --video_dir data/custom/videos/test_videos/ \
    --mode deep_learning \
    --model_path checkpoints/custom/best_model.pth \
    --device cuda \
    --output_dir results/batch_results/
```

### 2. 直接使用VLM推理（不需要训练）

```bash
# 单个视频VLM推理
python test_vlm_annotation.py

# 修改视频路径后运行
python -c "
from src.annotation.vlm_annotator import VLMAnnotator
from src.features.feature_extractor import FeatureExtractor
import json

# 提取特征
extractor = FeatureExtractor()
features = extractor.process_video('path/to/video.mp4', return_raw=True)

# VLM标注
annotator = VLMAnnotator(model='opus')
annotation = annotator.annotate_single_sample(features)

print(json.dumps(annotation, ensure_ascii=False, indent=2))
"
```

---

## ✅ 验证测试

### 1. 验证教师识别功能

```bash
# 测试教师识别
python test_teacher_detection.py

# 验证点：
# ✓ 每帧只有1条记录
# ✓ Y坐标平均值 < 300
# ✓ 文件大小减少约80%
```

### 2. 验证VLM标注质量

```bash
# 查看VLM标注的置信度分布
python -c "
import json
import numpy as np

with open('data/custom/vlm_annotations/annotations_batch.json') as f:
    data = json.load(f)
    confidences = [item['annotation']['confidence'] for item in data]

    print(f'样本数量: {len(confidences)}')
    print(f'平均置信度: {np.mean(confidences):.2f}')
    print(f'最低置信度: {min(confidences):.2f}')
    print(f'最高置信度: {max(confidences):.2f}')
    print(f'置信度 > 0.7: {sum(c > 0.7 for c in confidences)} 个')
"
```

### 3. 验证模型性能

```bash
# 在测试集上评估模型
python -c "
from src.models.deep_learning.train import evaluate_model
import torch
import json

# 加载模型和数据
model = torch.load('checkpoints/custom/best_model.pth')
with open('data/custom/training_data/training.json') as f:
    test_data = [x for x in json.load(f) if x['split'] == 'test']

# 评估
results = evaluate_model(model, test_data)
print(f'测试集准确率: {results[\"accuracy\"]:.2f}')
print(f'测试集F1分数: {results[\"f1\"]:.2f}')
"
```

### 4. 对比VLM vs Student模型

```bash
# 创建对比测试脚本
python data/custom/tools/validate_student_model.py \
    --model_path checkpoints/custom/best_model.pth \
    --test_videos data/custom/videos/test_videos/ \
    --output comparison_results.json

# 查看一致性
cat comparison_results.json | jq '.agreement_rate'
```

---

## 🛠️ 工具脚本

### 1. 清理中间文件

```bash
# 清理提取的特征
rm -rf data/custom/extracted_features/*.json

# 清理VLM标注
rm -rf data/custom/vlm_annotations/*.json

# 清理训练数据
rm -rf data/custom/training_data/*.json

# 清理模型检查点
rm -rf checkpoints/custom/*

# 清理日志
rm -rf logs/custom/*
```

### 2. 检查文件大小

```bash
# 检查特征文件大小
du -h data/custom/extracted_features/*.json

# 检查模型文件大小
du -h checkpoints/custom/*.pth

# 检查目录总大小
du -sh data/custom/extracted_features/
du -sh data/custom/vlm_annotations/
du -sh checkpoints/custom/
```

### 3. 统计视频数量

```bash
# 统计视频文件
find data/custom/videos/ -name "*.mp4" | wc -l

# 列出所有视频
find data/custom/videos/ -name "*.mp4" -exec basename {} \;

# 统计总时长（需要ffprobe）
find data/custom/videos/ -name "*.mp4" -exec ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 {} \; | awk '{s+=$1} END {print s/60 " 分钟"}'
```

### 4. Git提交

```bash
# 查看修改
git status

# 添加修改的文件（不包括大文件）
git add src/features/video_feature_extractor.py
git add test_teacher_detection.py
git add 教师识别功能说明.md
git add 启动命令汇总.md

# 提交
git commit -m "实现教师识别功能，优化特征文件大小

- 新增教师选择算法（位置+大小加权评分）
- 每帧只保留1条教师记录，数据压缩88%
- 文件大小从30MB减少到~5-7MB
- 添加测试脚本和文档

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# 推送到远程
git push origin fix/mediapipe-v0.10-api-upgrade
```

---

## 📊 常用组合命令

### 场景1: 快速测试单个视频

```bash
# 一条命令测试完整流程
python -c "
from src.features.feature_extractor import FeatureExtractor
from src.annotation.vlm_annotator import VLMAnnotator
import json

# 提取特征
print('1. 提取特征...')
extractor = FeatureExtractor()
features = extractor.process_video('data/custom/videos/test.mp4', return_raw=True)

# VLM标注
print('2. VLM标注...')
annotator = VLMAnnotator(model='opus')
annotation = annotator.annotate_single_sample(features)

# 输出结果
print('3. 结果:')
print(f'   风格: {annotation[\"annotation\"][\"style\"]}')
print(f'   置信度: {annotation[\"annotation\"][\"confidence\"]:.2f}')
print(f'   理由: {annotation[\"annotation\"][\"reasoning\"][:100]}...')
"
```

### 场景2: 完整训练流程（500个视频）

```bash
# 步骤1: 批量处理
bash data/custom/tools/process_custom_videos.sh

# 步骤2: 转换训练格式
python data/custom/tools/convert_to_training.py \
    --annotations data/custom/vlm_annotations/annotations.json \
    --features data/custom/extracted_features/features.json \
    --output data/custom/training_data/training.json \
    --min_confidence 0.5

# 步骤3: 训练模型
python -m src.models.deep_learning.train \
    --data_path data/custom/training_data/training.json \
    --model_config high_accuracy \
    --device cuda \
    --checkpoint_dir ./checkpoints/custom_500 \
    --log_dir ./logs/custom_500

# 步骤4: 评估模型
python data/custom/tools/validate_student_model.py \
    --model_path checkpoints/custom_500/best_model.pth \
    --test_videos data/custom/videos/test_videos/
```

### 场景3: 增量训练（添加更多数据）

```bash
# 处理新视频
python data/custom/tools/extract_features.py \
    --videos_dir data/custom/videos/new_batch \
    --output data/custom/extracted_features/features_new.json

# 标注新视频
python data/custom/tools/annotate_videos.py \
    --features_path data/custom/extracted_features/features_new.json \
    --output data/custom/vlm_annotations/annotations_new.json \
    --model opus

# 合并数据
python -c "
import json

# 加载旧数据
with open('data/custom/training_data/training.json') as f:
    old_data = json.load(f)

# 转换新数据
# ... (运行convert_to_training.py)

# 加载新数据
with open('data/custom/training_data/training_new.json') as f:
    new_data = json.load(f)

# 合并
combined = old_data + new_data

# 保存
with open('data/custom/training_data/training_combined.json', 'w') as f:
    json.dump(combined, f, ensure_ascii=False, indent=2)

print(f'总样本数: {len(combined)}')
"

# 重新训练
python -m src.models.deep_learning.train \
    --data_path data/custom/training_data/training_combined.json \
    --model_config high_accuracy \
    --device cuda \
    --checkpoint_dir ./checkpoints/custom_incremental
```

---

## ⚙️ 环境配置

### 确认依赖安装

```bash
# 检查Python环境
python --version  # 需要 Python 3.8+

# 检查关键依赖
python -c "
import torch
import cv2
import mediapipe
import whisper
import anthropic
print('✓ 所有依赖已安装')
"

# 如果缺少依赖，安装
pip install -r requirements.txt
```

### GPU检查

```bash
# 检查CUDA是否可用
python -c "
import torch
print(f'CUDA可用: {torch.cuda.is_available()}')
if torch.cuda.is_available():
    print(f'GPU数量: {torch.cuda.device_count()}')
    print(f'GPU名称: {torch.cuda.get_device_name(0)}')
"

# 查看GPU使用情况
nvidia-smi
```

### API配置

```bash
# 设置Anthropic API密钥
export ANTHROPIC_API_KEY="your-api-key-here"

# 或创建.env文件
echo "ANTHROPIC_API_KEY=your-api-key-here" > .env

# 验证API
python -c "
import anthropic
import os
client = anthropic.Anthropic(api_key=os.environ.get('ANTHROPIC_API_KEY'))
print('✓ API配置正确')
"
```

---

## 📚 相关文档

- **技术文档**: `data/custom/知识蒸馏技术文档.md`
- **快速开始**: `data/custom/QUICKSTART.md`
- **测试结果**: `data/custom/测试运行结果说明.md`
- **教师识别**: `教师识别功能说明.md`
- **API配置**: `data/custom/API_CONFIG.md`
- **完整README**: `data/custom/README.md`

---

## 🆘 故障排查

### 问题1: CUDA out of memory

```bash
# 减小batch_size
python -m src.models.deep_learning.train \
    --batch_size 8 \  # 从32减少到8
    --device cuda

# 或使用CPU
python -m src.models.deep_learning.train \
    --device cpu
```

### 问题2: VLM API错误

```bash
# 检查API密钥
echo $ANTHROPIC_API_KEY

# 检查配额
python -c "
import anthropic
import os
client = anthropic.Anthropic(api_key=os.environ.get('ANTHROPIC_API_KEY'))
# 尝试简单请求
response = client.messages.create(
    model='claude-opus-4-5-20251101',
    max_tokens=100,
    messages=[{'role': 'user', 'content': 'test'}]
)
print('✓ API工作正常')
"
```

### 问题3: 特征提取失败

```bash
# 检查视频文件
ffprobe data/custom/videos/test.mp4

# 检查MediaPipe
python -c "
import mediapipe as mp
print(f'MediaPipe版本: {mp.__version__}')
"

# 查看详细日志
python test_teacher_detection.py 2>&1 | tee debug.log
```

---

**文档版本**: 1.0
**更新时间**: 2026-01-13
**适用版本**: 教师风格分析系统 v0.10+
